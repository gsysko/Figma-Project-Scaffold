import * as React from 'react';
import './ui.css'

class About extends React.Component {
    render() {
        return (<>
          <h4>Ztart has already been run on this document.</h4>
          <p>Run Ztart on a new document to auto-generate a thumbnail and a set of recommended pages that you can edit, delete, or add to.</p>
          </>);
      }
}

export default About